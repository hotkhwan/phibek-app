apiVersion: apps/v1
kind: Deployment
metadata:
  name: phibek-app
  namespace: gw
  labels:
    app: phibek-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: phibek-app
  template:
    metadata:
      labels:
        app: phibek-app
    spec:
      imagePullSecrets:
      - name: harbor-credential
      containers:
      - name: phibek-app
        image: regis.pointit.co.th/phibek/phibek-app:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 3001
        readinessProbe:
          httpGet:
            path: /
            port: 3001
          initialDelaySeconds: 5
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /
            port: 3001
          initialDelaySeconds: 15
          periodSeconds: 20
        resources:
          limits:
            cpu: "500m"
            memory: "512Mi"
          requests:
            cpu: "100m"
            memory: "128Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: phibek-app
  namespace: gw
spec:
  selector:
    app: phibek-app
  ports:
  - port: 3001
    targetPort: 3001
  type: ClusterIP
